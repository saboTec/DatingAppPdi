server {
    listen 80;

    location /datingapp/ {
        proxy_pass http://api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Optional: fallback
    location / {
        return 404;
    }
}

# server {
#     listen 80;
#     server_name sabotec.at;

#     # Redirect to HTTPS if needed
#     # return 301 https://$host$request_uri;

#     # Main app under /sepioo
#     location /sepioo/ {
#         proxy_pass         http://api/;  # use your backend service name here
#         proxy_http_version 1.1;
#         proxy_set_header   Host $host;
#         proxy_set_header   X-Real-IP $remote_addr;
#         proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;

#         # Support Angular routing (e.g. /sepioo/home)
#         try_files $uri $uri/ /index.html;
#     }

#     # Fallback to existing website or 404
#     location / {
#         root /var/www/html; # path to your current website
#         index index.html;
#     }
# }